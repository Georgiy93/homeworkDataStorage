<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.netology.homeworkDataStorage"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.homeworkDataStorage"
        tools:targetApi="31">
        <activity
            android:name="ru.netology.homeworkDataStorage.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ru.netology.homeworkDataStorage.activity.IntentHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <activity
            android:name="ru.netology.homeworkDataStorage.activity.NewPostActivity"
            android:exported="false" />

        <activity
            android:name="ru.netology.homeworkDataStorage.activity.EditPostActivity"
            android:exported="false"

            />


    </application>


</manifest>